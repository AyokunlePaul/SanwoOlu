name: Build
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  unitTest:
    name: Run unit tests
    runs-on: ubuntu-latest

    steps:
      ## run unit tests
      - name: Unit tests
      - uses: actions/checkout@v2
        with:
          java-version: 11
        run: sudo bash ./gradlew test --stacktrace

  buildApk:
    name: Build debug apk
    runs-on: ubuntu-latest

    steps:
      ## Initial setup.
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

      ## Generate debug apk
      - name: Assemble debug apk
        uses: actions/setup-java@v1
        with:
          java-version: 11
        run: sudo ./gradlew :app:assembleDebug --stacktrace